product-name: Pivotal_Single_Sign-On_Service
network-properties:
  network:
    name: pas
  service_network:
    name: services
  other_availability_zones:
    - name: ${az1}
    - name: ${az2}
    - name: ${az3}
  singleton_availability_zone:
    name: ${az1}
